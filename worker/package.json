{
  "name": "game-shelf-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test ./test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.52.0"
  }
}
