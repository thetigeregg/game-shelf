{
  "name": "game-shelf-server",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node --enable-source-maps --import tsx src/index.ts",
    "start:background-worker": "node --enable-source-maps --import tsx src/background-worker.ts",
    "test": "timeout 180s env NODE_ENV=test node --test --test-timeout=30000 --test-force-exit --import tsx src/*.test.ts src/**/*.test.ts",
    "test:coverage": "mkdir -p coverage && timeout 300s env NODE_ENV=test node --test --test-timeout=30000 --test-force-exit --experimental-test-coverage --test-coverage-lines=80 --test-coverage-functions=80 --test-coverage-branches=80 --test-coverage-include='src/**/*.ts' --test-coverage-exclude='src/**/*.test.ts' --test-reporter=spec --test-reporter=lcov --test-reporter-destination=stdout --test-reporter-destination=coverage/lcov.info --import tsx src/*.test.ts src/**/*.test.ts && node ../scripts/coverage/normalize-lcov-paths.mjs coverage/lcov.info server"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/middie": "^9.3.3",
    "@fastify/rate-limit": "^11.1.0",
    "dotenv": "^17.4.2",
    "express-rate-limit": "^8.6.1",
    "fastify": "^5.10.0",
    "fastify-plugin": "^6.0.0",
    "firebase-admin": "^14.2.0",
    "pg": "^8.22.0",
    "tsx": "^4.23.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/pg": "^8.20.0",
    "typescript": "^7.0.2"
  }
}
