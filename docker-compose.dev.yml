services:
  api:
    environment:
      CORS_ORIGIN: ${CORS_ORIGIN:-http://127.0.0.1:8100,http://localhost:8100,http://127.0.0.1:8080,http://localhost:8080}
      REQUIRE_AUTH: ${REQUIRE_AUTH:-false}
      MANUALS_PUBLIC_BASE_URL: http://127.0.0.1:8080/manuals
    ports:
      - '127.0.0.1:3000:3000'

  postgres:
    ports:
      - '127.0.0.1:5432:5432'

  hltb-scraper:
    ports:
      - '127.0.0.1:8788:8788'
